from django.db import models

# Legacy models moved to 'inventory' and 'bookings' apps.
# This file is kept as an entry point for the 'api' app, 
# but models are now managed in specific domain apps.
