auto-install-peers=true
shamefully-hoist=true
node-linker=hoisted

# pnpm 10+ aborts in non-TTY environments (Docker, CI) when it needs to
# remove the modules directory. Setting this to false skips the prompt.
confirm-module-purge=false
